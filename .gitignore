# Gradle
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar
!**/src/main/**/build/
!**/src/test/**/build/

# Android Studio / IntelliJ
.idea/
*.iml
.cxx/
captures/
local.properties

# Android build outputs
/app/release/
/app/debug/
/app/build/
*.apk
*.aab
*.aar
*.ap_

# Kotlin
.kotlin/

# macOS / Linux / Windows
.DS_Store
Thumbs.db

# Logs
*.log
# Build log dumps accidentally saved to the repo root
/build_*.txt
/*_build.txt

# Android Studio / IntelliJ inspection profiles dumped to the repo root by mistake
# (these belong under .idea/inspectionProfiles/, which is already ignored)
/*.xml

# Keystore (NEVER commit secrets)
*.jks
*.keystore
release.keystore.properties
# Exception: the debug keystore is committed on purpose so CI builds are
# signed identically. It is NEVER used for release / Play Store.
!debug.keystore

# Google Service Account — clave privada RSA. NUNCA commitear.
app/src/main/res/raw/credentials.json
