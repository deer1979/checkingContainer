# Compose compiler stability configuration.
# List fully-qualified third-party class names (one per line) that should be
# treated as stable even though the compiler cannot verify them statically.
# Our own domain models (all-val data classes) are inferred stable automatically.
# Add entries here only when CI reports unstable-param warnings for external types.
