// Clases tratadas como estables por el compilador de Compose.
// core/model no es un módulo Compose, por lo que sus data classes (solo val)
// se inferirían inestables en los módulos feature sin esta declaración.
com.checkingcontainer.core.model.*
